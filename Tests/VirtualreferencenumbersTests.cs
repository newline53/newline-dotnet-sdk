//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using Newline53.Sdk;
using Newline53.Sdk.Models;
using Newline53.Sdk.Models.Components;
using Newline53.Sdk.Models.Errors;
using Newline53.Sdk.Models.Requests;
using Newline53.Sdk.Utils;
using Models = Newline53.Sdk.Models;
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Numerics;
using System.Threading.Tasks;
using Xunit;

public class VirtualreferencenumbersTests
{

    [Fact]
    public async Task TestVirtualreferencenumbers_GetVirtualReferenceNumbersVirtualReferenceNumbersList()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("get_/virtual_reference_numbers-virtual_reference_numbers_list");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        var res = await sdk.VirtualReferenceNumbers.ListAsync(
            instantPaymentRailRegistrationStatus: QueryParamInstantPaymentRailRegistrationStatus.Registered,
            status: GetVirtualReferenceNumbersQueryParamStatus.Active,
            syntheticAccountUid: "Dg1EPao8XukUpHG8",
            virtualReferenceNumber: "1234567890123456"
        );

        // handle response
        Assert.NotNull(res);
        Assert.Equal(200, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_PostVirtualReferenceNumbersCreatePayload()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("post_/virtual_reference_numbers-create_payload");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        PostVirtualReferenceNumbersRequest req = new PostVirtualReferenceNumbersRequest() {
            ExternalUid = "YrfDrfVRgpPgnhF5",
            Name = "greenfield1",
            SyntheticAccountUid = "Dg1EPao8XukUpHG8",
            RoutingNumber = "123456789",
        };

        var res = await sdk.VirtualReferenceNumbers.CreateAsync(req);

        // handle response
        Assert.NotNull(res);
        Assert.Equal(201, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_PostVirtualReferenceNumbersPending()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("post_/virtual_reference_numbers-pending");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        PostVirtualReferenceNumbersRequest req = new PostVirtualReferenceNumbersRequest() {
            ExternalUid = "partner-generated-id",
            Name = "greenfield1",
            SyntheticAccountUid = "Dg1EPao8XukUpHG8",
            RoutingNumber = "123456789",
        };

        var res = await sdk.VirtualReferenceNumbers.CreateAsync(req);

        // handle response
        Assert.NotNull(res);
        Assert.Equal(201, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_GetVirtualReferenceNumbersUidRegistered()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("get_/virtual_reference_numbers/{uid}-registered");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        var res = await sdk.VirtualReferenceNumbers.GetAsync(uid: "<id>");

        // handle response
        Assert.NotNull(res);
        Assert.Equal(200, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_PutVirtualReferenceNumbersUidRegistered()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("put_/virtual_reference_numbers/{uid}-registered");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        var res = await sdk.VirtualReferenceNumbers.UpdateAsync(
            uid: "<id>",
            body: new PutVirtualReferenceNumbersUidRequestBody() {
                ExternalUid = "partner-generated-id",
                Name = "greenfield1",
            }
        );

        // handle response
        Assert.NotNull(res);
        Assert.Equal(200, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_PutVirtualReferenceNumbersUidLockLocked()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("put_/virtual_reference_numbers/{uid}/lock-locked");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        var res = await sdk.VirtualReferenceNumbers.LockAsync(
            uid: "<id>",
            body: new PutVirtualReferenceNumbersUidLockRequestBody() {
                LockReason = LockReasonRequest.Admin,
            }
        );

        // handle response
        Assert.NotNull(res);
        Assert.Equal(200, (int)res.HttpMeta.Response.StatusCode);
    }

    [Fact]
    public async Task TestVirtualreferencenumbers_PutVirtualReferenceNumbersUidUnlockRegistered()
    {

        var testHttpClient = CommonHelpers.CreateTestHTTPClient("put_/virtual_reference_numbers/{uid}/unlock-registered");
        var sdk = new NewlineSDK(
            serverUrl: Environment.GetEnvironmentVariable("TEST_SERVER_URL") ?? "http://localhost:18080",
            security: new Security() {
                ProgramUid = "NEWLINE_PROGRAM_UID",
                HmacKey = "NEWLINE_HMAC_KEY",
            },
            client: testHttpClient
        );
        var res = await sdk.VirtualReferenceNumbers.UnlockAsync(uid: "<id>");

        // handle response
        Assert.NotNull(res);
        Assert.Equal(200, (int)res.HttpMeta.Response.StatusCode);
    }
}
