# TypeRequest

The type of VRN. Defaults to `business` if omitted. `consumer` is only available if enabled for your program.


## Example Usage

```csharp
using Newline53.Sdk.Models.Requests;

var value = TypeRequest.Business;
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `Business` | business   |
| `Consumer` | consumer   |